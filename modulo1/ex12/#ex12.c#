#include <ctype.h>

void capitalize(char *str){

  while(*str){
    while(*str && *str == ' '){
      
      str++;

      *str = toupper(*str);

      str++;

    while(*str && *str != ' ')

	str++;          
  }
}
